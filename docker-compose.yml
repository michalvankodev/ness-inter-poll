# see compatibility table: https://github.com/docker/compose/releases
# see networking info: https://docs.docker.com/compose/networking/
version: '3'
services:
  mongo:
    build:
      context: ./mongo
    #image: "mongo_db"
     #user: root    
    #container_name: "mongo-db"
    ports:        
        - "27017:27017" 
    #volumes:
        #- mongodata:c:\Docker_MongoData\
  nesspoll-users:
    build:
      context: ./services/users  
    #image: "nesspoll_users"
     #user: root
    ports:
        - "3001:3001"     
    #container_name: "nesspoll-users"   
    depends_on:
      - "mongo"    
  nesspoll:
    build:
      context: ./nesspoll
    #image: "nesspoll_web"    
    ports:
      - "3000:3000"      
    #container_name: "nesspoll-web"
    depends_on:
      - "nesspoll-users"   
#volumes:
    #mongodata:
  
    
